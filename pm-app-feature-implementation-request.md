# 卒制チームページ 機能追加 実装依頼(Claude Code用)

このファイルをリポジトリのルートに置き、Claude Codeに
「このファイルを読んで、書いてある内容に沿って実装してください」
と伝えて使ってください。フェーズごとに区切って依頼するのがおすすめです
(例:「まずフェーズ1だけ実装して」)。

## 前提・背景

- リポジトリ: `Bachi-ra/pm-app`(公開URL: https://bachi-ra.github.io/pm-app/）
- 構成: サーバーレス。ブラウザから直接Firebase Firestoreに接続。
  GitHub Pagesで `/docs` を配信。ビルドツールなし、素のES Modules。
- 主要ファイル:
  - `docs/index.html`
  - `docs/js/app.js`(全体のタブ切り替え・エントリポイント)
  - `docs/js/api.js`(Firestoreとの読み書き)
  - `docs/js/dashboard.js` / `tasks.js` / `gantt.js` / `members.js` /
    `memos.js` / `links.js` / `export.js` / `firebaseClient.js` / `utils.js`
  - `docs/css/style.css`
  - `docs/manifest.json` / `docs/sw.js`(PWA関連)
  - `firestore.rules`
- 設計パターン: 各タブは `renderXxx(container, ctx)` という関数で描画され、
  `ctx` に `members` / `tasks` / `milestones` / `currentMember` / `isAdmin` /
  `goToTab` などが渡される。`utils.js` に `escapeHtml` / `formatDate` /
  `roleColor` / `EVERYONE_ROLE` / `STATUS_LIST` などの共通処理がある。
- 権限モデル: PM(管理者)は全操作可能。一般メンバーは自分の役職
  (または「全員」)が割り当てられたタスクの状態/進捗更新、メモの投稿・
  自分の投稿の編集削除のみ可能。権限チェックはクライアント側で行っている
  (パスワード認証はなし)。
- Firestoreの既存コレクション: `members` / `tasks` / `milestones` /
  `memos` / `links`

## 進め方についての指示

1. 実装前に必ず現在のコード(特に `utils.js` / `app.js` / 各タブのjs /
   `firestore.rules`)を読み、既存の命名規則・関数構成・CSSクラス・UIの
   見た目(バッジ、カード、テーブル、モーダルの出し方)に合わせること。
   新しいUIパーツも既存の `style.css` のデザイントークンを流用し、
   独自の見た目を増やしすぎない。
2. 新しいFirestoreコレクション/フィールドを追加する場合は
   `firestore.rules` も必ず更新する。
3. 機能ごとに独立したタスクとして扱い、1機能ずつ
   「実装 → `npx serve docs` などでローカル確認 → コミット」という単位で
   進める(全部を1コミットにまとめない)。コミットメッセージは日本語で
   機能単位がわかるようにする。
4. UIの文言はすべて既存に合わせて日本語。絵文字は使わない。
5. 外部ライブラリやビルドツール(React、webpackなど)は導入しない方針を
   維持する。どうしても必要な場合は、導入前に理由を説明してから提案する。
6. 権限まわり(PMのみ編集可能、等)は既存の `isAdmin` /
   `currentMember` の考え方を踏襲する。
7. 各機能の実装が終わったら、READMEの「使い方」セクションに簡潔に追記する。
8. 仕様に曖昧な点や、Firebase側の追加設定(Cloud Messagingの有効化、
   Webhook用URLの発行など)がユーザー側の手動作業を要する場合は、
   実装を進める前に質問・案内すること。

## フェーズ1: タスク管理の強化(優先度: 高)

**1. サブタスク/チェックリスト**
- `tasks` の各ドキュメントに `checklist: [{id, text, done}]` を追加
- タスク編集モーダル(`tasks.js`)にチェックリストの追加/削除/
  チェックUIを追加
- タスク一覧の詳細表示に「チェックリスト 2/5 完了」のような進捗表示を追加

**2. タスクの優先度**
- `tasks` に `priority: '高' | '中' | '低'`(デフォルト`'中'`)を追加
- タスク一覧にフィルタと優先度バッジを追加
  (色分け: 高=赤系、中=黄系、低=グレー系。`utils.js` に
  `roleColor` と同様の関数を追加する)
- ダッシュボードの「直近7日以内の締切」を優先度順にソートできるようにする

**3. タスクの依存関係**
- `tasks` に `dependsOn: [taskId, ...]` を追加
- タスク編集モーダルで依存タスクを選択できるUIを追加(自分自身は選択不可)
- ガントチャート(`gantt.js`)で依存関係を可視化する
  (初期実装はバーの下に「依存: ○○タスク」とテキスト表示するだけでも可)
- 依存元タスクが未完了の場合、依存先タスクに警告バッジ
  「先行タスク未完了」を表示する

**4. タスクごとのコメント欄**
- 新コレクション `taskComments`
  (フィールド: `taskId`, `authorMemberId`, `text`, `createdAt`)を追加
- タスク詳細/編集モーダルにコメント一覧+投稿フォームを追加
  (メモ機能のUIパターンを流用してよい)
- 自分のコメントは編集・削除可、PMは全コメント削除可
  (既存のメモ機能と同じ権限モデル)
- `firestore.rules` に `taskComments` のルールを追加

## フェーズ2: 映像制作特化機能(優先度: 高)

**5. カット/バージョン管理**
- 新コレクション `versions`
  (フィールド: `taskId`, `versionLabel`(例:`v1`), `url`
  (Google Drive等の外部リンク), `note`,
  `status`('レビュー待ち'|'差し戻し'|'承認'), `createdBy`, `createdAt`)を追加
- タスク詳細にバージョン一覧を追加し、バージョンごとにURL・メモ・
  ステータスを表示・追加できるようにする
- PMまたはタスク担当者がステータスを変更できるようにする
- フィードバックは4のコメント機能を流用してよい

**6. 素材・権利管理表**
- 新コレクション `assets`
  (フィールド: `name`, `type`('BGM'|'SE'|'フォント'|'画像'|'その他'),
  `source`(入手元URL等), `license`(ライセンス/利用規約の状況),
  `usedInTaskId`(任意), `note`)を追加
- 新タブ「素材」を追加し、一覧表示・追加・編集・削除ができるようにする
  (削除はPMのみ、追加・編集は全員可とするかは実装時に確認してよい)
- ライセンス未確認のものには警告バッジを表示する

**7. 絵コンテ/参考資料ギャラリー**
- 新コレクション `references`
  (フィールド: `title`, `imageUrl`, `note`, `tags`, `uploadedBy`,
  `createdAt`)を追加(画像は外部ホスティングのURLを貼る想定。
  Firebase Storageは使わずリンク形式でよい)
- 新タブ「資料」を追加し、画像をサムネイル表示するギャラリービューを作る
- タグでの絞り込みができるとなお良い

## フェーズ3: スケジュール・通知(優先度: 中)

**8. カレンダービュー**
- スケジュールタブ内にガントチャートと切り替えられる月表示カレンダーを追加
- タスクの締切日とマイルストーンを日付セルに表示する

**9. Googleカレンダー連携(.ics出力)**
- `export.js` を拡張し、マイルストーン(可能であればタスク締切も)を
  `.ics` 形式でダウンロードできるボタンをスケジュールタブに追加する

**10. 締切リマインド通知**
- まずは「アプリを開いたときにブラウザ通知を出す」方式
  (Notification API、Service Worker経由)から着手する
- 締切が近い(例: 3日以内)かつ未完了のタスクがある場合、ダッシュボード
  表示時に、通知許可済みであれば通知を出す
- Firebase Cloud Messagingでのプッシュ通知(アプリを開いていなくても
  届く方式)は追加のFirebase設定が必要になるため、要否をユーザーに
  確認してから着手する
- Discord Webhook連携も選択肢として提示し、必要であれば
  Webhook URLの設定欄をリンク集タブ等に追加する形で実装する

## フェーズ4: 可視化・検索(優先度: 中)

**11. バーンダウン/バーンアップチャート**
- ダッシュボードに、日付ごとの「残タスク数」または「進捗率の平均」推移を
  シンプルな折れ線グラフ(SVGで自前実装、追加ライブラリなし)で表示する
- マイルストーン(例: 提出日)を目標ラインとして表示する

**12. 「自分のタスクだけ」ビュー**
- タスク一覧に「自分の担当のみ表示」トグルボタンを追加する
  (`currentMember` の役職でフィルタするショートカット)

**13. 検索機能**
- ヘッダー等に検索ボックスを追加し、タスクのタイトル/カテゴリ、
  メモの本文、リンクのタイトルを横断検索して結果を表示する簡易実装でよい

## フェーズ5: 運用・管理(優先度: 低〜中)

**14. 変更履歴(アクティビティログ)**
- 新コレクション `activityLog`
  (フィールド: `type`(例: `task_status_changed`), `targetId`,
  `memberId`, `detail`, `createdAt`)を追加
- タスクのステータス/進捗変更、メンバーの追加・削除など主要操作時に
  ログを1件書き込む
- ダッシュボードまたは新タブに直近のアクティビティ一覧を表示する

**15. 閲覧専用リンク/モード**
- URLクエリパラメータ(例: `?readonly=1`)でアクセスした場合、すべての
  編集・追加・削除ボタンを非表示にする閲覧専用モードを実装する
- 「閲覧専用URLをコピー」ボタンをどこかに設置する

**16. データのJSONバックアップ/インポート**
- 管理者向けに、5コレクションの全データをJSON形式でエクスポートする
  ボタンを追加する(`members.js` または新規の管理タブに設置)
- インポート機能(JSONを読み込んでFirestoreに書き戻す)も実装するが、
  誤操作防止のため「本当に上書きしますか」の確認ダイアログを必須にする

## 補足

- 全部を一度に実装すると影響範囲が大きくレビューが大変になるため、
  上記フェーズごとに区切って進めてください。1フェーズ終わるごとに、
  変更点の要約と、ユーザー側に追加でしてもらう必要がある作業
  (Firebaseコンソールでの設定変更など)があれば教えてください。
- 既存の「身内の小規模チーム向け」というシンプルな設計思想を壊さない
  ようにしてください。過剰に複雑な権限管理や外部サービス連携は避け、
  まずは動くものを優先してください。
