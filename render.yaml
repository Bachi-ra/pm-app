services:
  - type: web
    name: pm-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: pmapp
      - key: NODE_OPTIONS
        value: --tls-max-v1.2
