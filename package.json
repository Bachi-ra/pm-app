{
  "name": "pm-app",
  "version": "1.0.0",
  "description": "卒業制作 チームページ(Firestore + GitHub Pages)",
  "private": true,
  "scripts": {
    "migrate": "node scripts/migrate-mongo-to-firestore.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0"
  }
}
